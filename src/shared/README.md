# shared

프로젝트 전역에서 재사용하는 코드를 두는 레이어입니다. 특정 비즈니스 로직에
의존하지 않습니다.

- `ui/` — 공용 UI 컴포넌트 (디자인 시스템)
- `api/` — HTTP 클라이언트 등 API 인프라
- `styles/` — 디자인 토큰, 전역 스타일 (vanilla-extract)
- `lib/` — 공용 유틸리티
- `config/` — 전역 설정, 상수
