/** 선택 시 blink 애니메이션 길이. 선택 후 진행 타이밍을 맞출 때 함께 쓴다. */
export const CHOICE_PANEL_BLINK_DURATION_MS = 240;

/** 선택 blink가 끝나고 잠깐 머문 뒤 다음 단계로 넘어가기까지의 지연. */
export const CHOICE_PANEL_ADVANCE_DELAY_MS = CHOICE_PANEL_BLINK_DURATION_MS + 360;
